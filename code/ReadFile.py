import math
import random
import collections

def __init__(self):
    pass

def csv(self,fname,fun,sep,src,s,t):
    src = fname
    while true:
        with open(src) as f:
            s = f.read()
            if s:
                t = {}
                for s1 in s.split(','):
                    t[1+len(t)] = coerce(s1)


def coerce(s,fun):
    def fun(s1):
        if s1:
            return true
        else:
            return false
        return s1
    return s.isnumeric()

 